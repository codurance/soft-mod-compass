const getFormSubmission = (uuid) => {};
const getContactId = (email) => {};
const uploadFile = (file, pathOnHubspotFilemanager) => {};
const createNote = (
  contactId,
  attachmentsIds,
  ownerId,
  timestamp = Date.now()
) => {};
module.exports = {
  getFormSubmission,
  getContactId,
  uploadFile,
  createNote,
};
